# Guide for pushing and pulling in this pipeline:
# https://gitlab.com/guided-explorations/gitlab-ci-yml-tips-tricks-and-hacks/commit-to-repos-during-ci/commit-to-repos-during-ci/-/blob/master/.gitlab-ci.yml
#
# To prevent race conditions and ensure all artifacts are perfectly versioned,
# this pipeline is divided into two distinct, sequential phases:
#
# 1. MERGE REQUEST PIPELINE (Triggered on MRs to `main` or `release`):
#    - Runs environment setup, pre-commit linting, and unit tests.
#    - Builds and pushes the Docker image to the registry.
#    - Bumps the code version and pushes a new Git tag (triggers Phase 2).
#
# 2. TAG PIPELINE (Triggered *only* when a new Git tag is pushed):
#    - Builds and deploys the versioned documentation (GitLab Pages).
#    - Publishes the compiled Python package to the Package Registry.
#    - Generates the official GitLab Release page.
#
# Note: We use the top-level 'workflow' keyword below to define these global
# entry points, avoiding the need to repeat MR/Tag rules in every single job.


# ==========================================
# NEXTGENFRAMEWORK IMPORTS
# NOTE: whoever runs the pipeline must be granted at least a 'Reporter' role in the nextgen_framework project
# ==========================================

include:
  - project: 'nextgeninfra/nextgen_framework'
    ref: 'main'
    file: '/templates/aiqu.yml'


# ==========================================
# GLOBAL CONFIGURATION & TEMPLATES
# ==========================================

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event' && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == 'main' || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == 'release')
    - if: $CI_PIPELINE_SOURCE == 'web'
    - if: $CI_COMMIT_TAG # tag pipelines

stages:
  - .pre
  - setup
  - test
  - version
  - build
  - release
  - deploy


# --- GLOBAL VARIABLES ---

variables:
    HTTPS_REMOTE: https://oauth2:$CI_REPO_ACCESS@$CI_SERVER_HOST/$CI_PROJECT_PATH.git
    GIT_DEPTH: 0
    CUDA_VISIBLE_DEVICES: ""
    UV_CACHE_DIR: "$CI_PROJECT_DIR/.uv-cache"
    UV_LINK_MODE: "copy"
    UV_COMPILE_BYTECODE: "0"
    # Naming convention is UV_INDEX_<NAME_OF_INDEX>_USERNAME, see pyproject.toml for NAME_OF_INDEX
    UV_INDEX_NEXTGEN_USERNAME: "gitlab-ci-token"
    UV_INDEX_NEXTGEN_PASSWORD: "${CI_JOB_TOKEN}"


# ==========================================
#  MAINTENANCE
# ==========================================


aggressive-docker-prune:
  stage: .pre  # Or any stage that makes sense, but keeping it isolated is good
  tags:
    - shell
  rules:
    - when: manual
  allow_failure: true
  script:
    # Delete lingering Buildx instances
    # Every time we run `docker buildx create`, it spins up a hidden container.
    # This wipes all of them out so they don't stack up infinitely. If caching locally, this kills that advantage.
    - echo "Removing old Buildx builders..."
    - for builder in $(docker buildx ls | awk '/docker-container/ {print $1}'); do docker buildx rm -f $builder; done || true

    # The standard system nuke
    # -a: Removes ALL unused images, not just dangling ones.
    # --volumes: Wipes out orphaned volumes that containers left behind.
    # -f: Force, no yes/no prompts.
    - echo "Pruning system, images, containers, and volumes..."
    - docker system prune -a -f --volumes

    # The BuildKit Exterminator
    # BuildKit stores its cache completely separately from standard Docker images.
    # This explicitly targets the internal BuildKit storage.
    - echo "Pruning BuildKit internal caches..."
    - docker builder prune -a -f

    # Space diagnostics on this host
    - echo "1. Checking GitLab Runner Builds & Caches:"
    - du -sh $CI_BUILDS_DIR/../../* 2>/dev/null | sort -rh | head -n 5 || true

    - echo "2. Checking Host User Cache (~/.cache):"
    - du -sh ~/.cache/* 2>/dev/null | sort -rh | head -n 5 || true

    - echo "3. Checking Docker Internal Folders:"
    - sudo du -sh /var/lib/docker/* 2>/dev/null | sort -rh | head -n 5 || true

    - echo "4. Checking System Logs:"
    - sudo du -sh /var/log/* 2>/dev/null | sort -rh | head -n 5 || true

    - echo "Cleanup and diagnostics complete! Current disk space:"
    - df -h /var/lib/docker || df -h /


# ==========================================
#  MERGE REQUEST PIPELINE
# ==========================================


# Requires GIT_SSL_CAINFO to be set in CI/CD Settings > Variables (either at project or group level)
build-ci-image:
  stage: .pre
  tags:
    - shell
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_JOB_TOKEN" $CI_REGISTRY

    # Generate the official BuildKit config using the direct path to the CA cert
    - |
      cat <<EOF > buildkitd.toml
      [registry."gitlab.mgmt.ai.se:5050"]
        ca=["${GIT_SSL_CAINFO}"]
      EOF

    # Pass the config file directly into the isolated builder
    - export BUILDER_NAME="nextgen-shared-builder"
    - docker buildx inspect $BUILDER_NAME > /dev/null 2>&1 || docker buildx create --name $BUILDER_NAME --driver docker-container --config buildkitd.toml
    - docker buildx use $BUILDER_NAME
    - docker buildx inspect --bootstrap

  # NOTE: provenance metadata can mess with the Gitlab UI -> turn off unless required!
  script:
    - echo "Building custom CI image from docker/ci.Dockerfile..."
    - |
      docker buildx build --push \
        --provenance=false \
        --secret id=internal_ca,src="${GIT_SSL_CAINFO}" \
        --cache-from type=registry,ref="$CI_REGISTRY_IMAGE/ci-image:buildcache" \
        --cache-to type=registry,ref="$CI_REGISTRY_IMAGE/ci-image:buildcache",mode=max \
        -f docker/ci.base.Dockerfile \
        -t "$CI_REGISTRY_IMAGE/ci-image:latest" .
  after_script:
    - echo "Cleaning up local runner ..."
    - docker image prune -f
    - docker builder prune -f
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_PIPELINE_SOURCE == 'web'


# Gets the starting code version when pipeline is run. Sets `APP_VERSION` AND `TAG_VERSION`
get-initial-version:
  stage: .pre
  image:
    name: $CI_REGISTRY_IMAGE/ci-image:latest
    pull_policy: always
  needs: ["build-ci-image"]
  script:
    - RAW_VERSION=$(poe get-latest-tag)
    - echo "Found $RAW_VERSION"
    - echo "APP_VERSION=${RAW_VERSION#v}" >> version.env
    - echo "TAG_VERSION=$RAW_VERSION" >> version.env
  artifacts:
    reports:
      dotenv: version.env
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_PIPELINE_SOURCE == 'web'


.tools_setup:
  image:
    name: $CI_REGISTRY_IMAGE/ci-image:latest
    pull_policy: always
  before_script:
    - git config --global user.email "${GITLAB_USER_EMAIL}"
    - git config --global user.name "${GITLAB_USER_NAME}"
    # uv sync creates the .venv and installs everything instantly from uv.lock
    - uv sync --frozen
    - source .venv/bin/activate
    # View environment
    - git config --list
    - env


# Template for the FIRST job that creates and pushes the cache.
.cache_producer:
  extends: .tools_setup
  cache:
    key:
      files:
        - uv.lock
    paths:
      - .venv
      - .uv-cache
    policy: pull-push
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_PIPELINE_SOURCE == 'web'



# Template for subsequent jobs that only CONSUME the cache.
.cache_consumer:
  extends: .tools_setup
  cache:
    key:
      files:
        - uv.lock
    paths:
      - .venv
      - .uv-cache
    policy: pull
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_PIPELINE_SOURCE == 'web'



setup:
  extends: .cache_producer
  stage: setup
  needs: ["build-ci-image"]
  script:
    - echo "Syncing dependencies to build the cache..."
    # This single command downloads everything to .uv-cache and builds the .venv
    - uv sync --all-extras --dev


run-precommit:
  extends: .cache_consumer
  stage: test
  script:
    - echo "Running hooks..."
    - uv run pre-commit run --all-files


# NOTE: This runs on MRs AND Tag pipelines, otherwise artifacts won't be made available for docs publishing
run-tests:
  extends: .cache_consumer
  stage: test
  script:
    - echo "Running tests..."
    - uv run poe test 4
    - uv run poe export-test-artifacts --directory docs/exported
  artifacts:
    paths:
      - docs/exported/
    expire_in: 1 hour
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_PIPELINE_SOURCE == 'web'
    - if: $CI_COMMIT_TAG


version-and-tag:
  extends: .cache_consumer
  stage: version
  script:
    - echo "Running code versioning..."
    - '[ "$CI_MERGE_REQUEST_TARGET_BRANCH_NAME" = "main" ] && part=patch || part=minor'
    # NOTE: this MUST set "BUMPED_VERSION" into version.env!
    - uv run poe bump-version-tag ${part} --annotate
    - echo "Pushing new tag to remote..."
    - export NEW_TAG=$(git describe --tags --abbrev=0)
    - git push $HTTPS_REMOTE $NEW_TAG
  artifacts:
    reports:
      # This overwrites the version.env from get-initial-version with the new, bumped version
      dotenv: version.env


# Build and push the Docker image to the GitLab Container Registry.
# NOTE: the two jobs are bundled here, because the image is too large for passing around as a .tar
build-and-push-image:
  stage: build
  tags:
    - shell
  needs:
    - job: get-initial-version  # Gets $APP_VERSION
    - job: version-and-tag      # Gets $BUMPED_VERSION (if the job ran)
  script:
    # Unset the DOCKER_HOST variable to force the client to use the local daemon.
    - unset DOCKER_HOST
    # Login to the GitLab Container Registry using a short-lived, secure job token.
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_JOB_TOKEN" $CI_REGISTRY

    # Generate the official BuildKit config to trust the internal CA cert
    - |
      cat <<EOF > buildkitd.toml
      [registry."gitlab.mgmt.ai.se:5050"]
        ca=["${GIT_SSL_CAINFO}"]
      EOF

    - FINAL_VERSION=${BUMPED_VERSION:-$APP_VERSION}
    - echo "Building Docker image with code version $FINAL_VERSION..."

    # Spin up the shared builder and feed it the config file
    - export BUILDER_NAME="nextgen-shared-builder"
    - docker buildx inspect $BUILDER_NAME > /dev/null 2>&1 || docker buildx create --name $BUILDER_NAME --driver docker-container --config buildkitd.toml
    - docker buildx use $BUILDER_NAME
    - docker buildx inspect --bootstrap

    # Build the image and tag it securely
    # NOTE: provenance metadata can mess with the Gitlab UI -> turn off unless required!
    - |
      docker buildx build --push \
        --provenance=false \
        --secret id=internal_ca,src="${GIT_SSL_CAINFO}" \
        --secret id=uv_username,env=UV_INDEX_NEXTGEN_USERNAME \
        --secret id=uv_password,env=UV_INDEX_NEXTGEN_PASSWORD \
        --build-arg VERSION="$FINAL_VERSION" \
        --cache-from type=registry,ref="$CI_REGISTRY_IMAGE:buildcache" \
        --cache-to type=registry,ref="$CI_REGISTRY_IMAGE:buildcache",mode=max \
        -f docker/Dockerfile \
        -t "$CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA" \
        -t "$CI_REGISTRY_IMAGE:latest" .

  after_script:
    - echo "Cleaning up local runner ..."
    - docker image prune -f
    - docker builder prune -f
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_PIPELINE_SOURCE == 'web'


tag_major_release:
  extends: .cache_consumer
  stage: release
  when: manual
  script:
    - uv run poe bump-version-tag major --annotate
    - export NEW_TAG=$(git describe --tags --abbrev=0)
    - git push $HTTPS_REMOTE $NEW_TAG
  artifacts:
    reports:
      dotenv: version.env
  rules:
    - if: $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == 'release'


publish_major_release:
  stage: release
  image: registry.gitlab.com/gitlab-org/cli:latest
  needs:
    - job: tag_major_release
      artifacts: true
  before_script:
    # Copy the runner's injected CA cert into Alpine's trust store
    - cp $GIT_SSL_CAINFO /usr/local/share/ca-certificates/internal-ca.crt
    - apk add --no-cache ca-certificates
    - update-ca-certificates
  script:
    - echo "Publishing GitLab release for $BUMPED_TAG..."
  release:
    tag_name: "$BUMPED_TAG"
    description: 'Major release created by CI pipeline'
  rules:
    - if: $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == 'release'


# ==========================================
#  TAG PIPELINE
# ==========================================


build-docs:
  extends: .cache_consumer
  stage: build
  needs:
    - job: run-tests
      artifacts: true
  script:
    - echo "Running docs & versioning..."
    - git fetch origin
    - uv run poe docs --verbose
    - uv run poe docs-version --prefix public --branch pages
    - git checkout pages
  # Make material for Gitlab pages available
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_TAG


# Special job to deploy Gitlab pages - name is keyword
pages:
  stage: release
  needs: ["build-docs"]
  artifacts:
    paths:
      - public
  script:
    - echo "Deploying to GitLab Pages..."
  rules:
    - if: $CI_COMMIT_TAG


publish-package:
  extends: .cache_consumer
  stage: release
  script:
    - echo "Building the package..."
    # This creates the package files (a .whl and a .tar.gz) in a 'dist/' directory
    - uv build
    - echo "Publishing package to the GitLab Package Registry..."
    # The CI_JOB_TOKEN provides secure, short-lived authentication.
    - |
      uv publish dist/* \
      --publish-url "https://${CI_SERVER_HOST}/api/v4/projects/${CI_PROJECT_ID}/packages/pypi" \
      --username "gitlab-ci-token" \
      --password ${CI_JOB_TOKEN}
  rules:
    - if: $CI_COMMIT_TAG


# ==========================================
#  NEXTGEN AIQU INTEGRATION
# ==========================================


deploy-aiqu:
  stage: deploy
  extends: .deploy-node
  variables:
    # This URL is tightly coupled to how AiQU / the scheduler works: full URL vs. relative path to a whitelisted domain
    IMAGE: $CI_PROJECT_PATH:$CI_COMMIT_SHORT_SHA
    QID: 23
    STORAGE_ID: 225
    #STORAGE_SOURCE: dummy
    #STORAGE_TO: dummy
    MLFLOW_API_TOKEN: "$GITLAB_MLFLOW"
